# EcoTwin backend
numpy>=1.26          # physics in model.py
fastapi>=0.110       # REST API (backend/app.py)
uvicorn[standard]>=0.29  # ASGI server

# The Azure AI Foundry hook (backend/ai_client.py) uses only the standard
# library (urllib), so no extra SDK is required.
